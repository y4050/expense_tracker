<h2>Month: <%= monthName %></h2>
<% if (!expenses){ %> 
        <p>No Record</p>
    <% } else { %>
        <% expenses.forEach(function(expense) { %>
            <div>
                <li>
                    <%= expense.date.substring(5, 10).replace('-','/') %>
                    <%= expense.name %>
                    $<%= expense.amount %>

                    <% theSum += parseFloat(expense.amount) %>
                </li>
            </div>
        <% }) %>
    <% } %>  
<h3>Month's total: $<%= theSum.toFixed(2) %></h3>

<a href="/expense">Back To Main</a>